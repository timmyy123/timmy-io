export interface ProjectType {
  title: string;
  image: any;
  link: string
  description: string;
  stacks: string[];
}

export interface ExperienceType {
  company: string;
  role: string;
  time: string;
  description: string;
}